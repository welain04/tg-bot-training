# Deprecated: clinic texts are in config/clinic.toml (committed to git).
# Edit config/clinic.toml and push to main — GitHub Actions will deploy.
Write-Host "Clinic texts moved to config/clinic.toml"
Write-Host "Edit that file and push to main instead of using Fly secrets."
